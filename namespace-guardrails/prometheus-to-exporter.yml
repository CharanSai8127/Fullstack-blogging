apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: exporter-allow-prometheus
  namespace: blog-prod
spec:
  podSelector:
    matchLabels:
      app: mysqld-exporter
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: monitoring
          podSelector:
            matchLabels:
              app.kubernetes.io/name: prometheus
      ports:
        - protocol: TCP
          port: 9104
